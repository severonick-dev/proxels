{
  "name": "@proxels/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Общие типы, DTO, enums, константы между backend и frontend.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'no lint configured for shared yet'"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
